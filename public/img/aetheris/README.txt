Screenshots rendered by src/components/AetherisPage.astro. The filenames come
from `aetherisPage.screenshots[].file` in src/i18n/translations.ts, so adding a
screenshot means adding an entry there (in both locales) and dropping the file
here.

  overview.png   - Cluster overview
  resources.png  - Resource browser
  yaml.png       - YAML editor
  terminal.png   - Logs & pod terminal
