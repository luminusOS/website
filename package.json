{
  "name": "luminusos-website",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "dev:site": "astro dev",
    "build": "npm run build:site && npm run build:blog:site && npm run build:wiki:site && node scripts/add-embedded-routes.mjs",
    "build:site": "astro build",
    "build:aurora": "SITE_VARIANT=aurora astro build && node scripts/clean-aurora-build.mjs",
    "build:blog": "astro build --root blog",
    "build:blog:site": "SITE_BASE=/blog SITE_URL=https://luminusos.org astro build --root blog && rm -rf dist/blog && mkdir -p dist/blog && cp -R blog/dist/. dist/blog/",
    "build:wiki": "astro build --root wiki",
    "build:wiki:site": "SITE_BASE=/wiki SITE_URL=https://luminusos.org astro build --root wiki && rm -rf dist/wiki && mkdir -p dist/wiki && cp -R wiki/dist/. dist/wiki/",
    "preview": "npm run build && wrangler dev --config wrangler.worker.jsonc",
    "astro": "astro",
    "deploy": "npm run build && wrangler deploy --config wrangler.worker.jsonc",
    "deploy:aurora": "npm run build:aurora && wrangler deploy --config wrangler.aurora.jsonc",
    "deploy:aurora:ci": "wrangler deploy --config wrangler.aurora.jsonc",
    "deploy:blog": "npm run build:blog && wrangler deploy --config wrangler.blog.jsonc",
    "deploy:blog:ci": "wrangler deploy --config wrangler.blog.jsonc",
    "deploy:wiki": "npm run build:wiki && wrangler deploy --config wrangler.wiki.jsonc",
    "deploy:wiki:ci": "wrangler deploy --config wrangler.wiki.jsonc",
    "cf-typegen": "wrangler types --config wrangler.worker.jsonc"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.6.13",
    "@astrojs/starlight": "^0.36.0",
    "astro": "^5.8.0",
    "sharp": "^0.34.0"
  },
  "devDependencies": {
    "wrangler": "^4.107.0"
  }
}
