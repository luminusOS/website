---
import type { Lang } from '../i18n/translations';
import { translations, langPrefix, languages, links, mainSite, siteVariant, subHref, siteHref } from '../i18n/translations';

interface Props {
  lang: Lang;
  path: string;
}

const { lang, path } = Astro.props;
const t = translations[lang];
const p = langPrefix(lang);
const current = languages.find((l) => l.code === lang) ?? languages[0];

const nav = [
  { href: `${mainSite}${p}/#why`, label: t.nav.overview },
  { href: `${mainSite}${p}/#how`, label: t.nav.how },
  { href: `${mainSite}${p}/#editions`, label: t.nav.editions },
  { href: subHref(p, 'aurora', '/aurora', links.auroraSite), label: t.nav.auroraShort },
];

const products = [
  { href: `${mainSite}${p}/aetheris`, label: t.nav.aetheris },
];

const navAfter = [
  { href: siteHref('/wiki', links.wikiSite), label: t.nav.wiki },
  { href: siteHref('/blog', links.blogSite), label: t.nav.blog },
];
---

<header class="site-header">
  <div class="container header-inner">
    <a class="brand" href={siteVariant ? `${mainSite}${p}` || mainSite : p || '/'}>
      <img src="/logos/brand.svg" alt="" width="28" height="28" />
      <span>Luminus<strong>OS</strong></span>
    </a>

    <nav class="main-nav" aria-label="Main">
      {nav.map((item) => <a href={item.href}>{item.label}</a>)}
      <details class="nav-menu" data-nav-menu>
        <summary>
          {t.nav.products}
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" aria-hidden="true">
            <path d="m6 9 6 6 6-6" />
          </svg>
        </summary>
        <ul role="list">
          {products.map((item) => (
            <li><a href={item.href}>{item.label}</a></li>
          ))}
        </ul>
      </details>
      {navAfter.map((item) => <a href={item.href}>{item.label}</a>)}
    </nav>

    <div class="header-actions">
      <button class="theme-toggle" type="button" data-theme-toggle aria-label={t.nav.themeToggle} title={t.nav.themeToggle}>
        <svg class="icon-sun" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
          <circle cx="12" cy="12" r="4.5" />
          <path d="M12 2v2.5M12 19.5V22M2 12h2.5M19.5 12H22M4.6 4.6l1.8 1.8M17.6 17.6l1.8 1.8M19.4 4.6l-1.8 1.8M6.4 17.6l-1.8 1.8" />
        </svg>
        <svg class="icon-moon" viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M21 12.8A9 9 0 1 1 11.2 3a7 7 0 0 0 9.8 9.8z" />
        </svg>
      </button>

      <details class="lang-menu" data-lang-menu>
        <summary class="mono" aria-label={current.name}>
          <span class="flag" aria-hidden="true">{current.flag}</span>
          {lang === 'pt-br' ? 'PT' : 'EN'}
          <svg viewBox="0 0 24 24" width="11" height="11" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" aria-hidden="true">
            <path d="m6 9 6 6 6-6" />
          </svg>
        </summary>
        <ul role="list">
          {languages.map((l) => (
            <li>
              <a
                href={(langPrefix(l.code) + path) || '/'}
                aria-current={l.code === lang ? 'true' : undefined}
              >
                <span class="flag" aria-hidden="true">{l.flag}</span>
                {l.name}
              </a>
            </li>
          ))}
        </ul>
      </details>

      <details class="mobile-menu" data-mobile-menu>
        <summary aria-label="Menu">
          <svg viewBox="0 0 24 24" width="17" height="17" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" aria-hidden="true">
            <path d="M4 7h16M4 12h16M4 17h16" />
          </svg>
        </summary>
        <div class="mobile-panel">
          <nav aria-label="Mobile">
            {nav.map((item) => <a href={item.href}>{item.label}</a>)}
            {products.map((item) => <a href={item.href}>{item.label}</a>)}
            {navAfter.map((item) => <a href={item.href}>{item.label}</a>)}
          </nav>
          <div class="mobile-langs">
            {languages.map((l) => (
              <a
                href={(langPrefix(l.code) + path) || '/'}
                aria-current={l.code === lang ? 'true' : undefined}
              >
                <span class="flag" aria-hidden="true">{l.flag}</span>
                {l.name}
              </a>
            ))}
          </div>
          <div class="mobile-actions">
            <a class="btn btn-primary btn-sm" href={`${mainSite}${p}/#download`}>{t.nav.download}</a>
            <a class="btn btn-ghost btn-sm" href={links.donate} target="_blank" rel="noopener">{t.nav.donate}</a>
          </div>
        </div>
      </details>

      <a class="btn btn-primary btn-sm" href={`${mainSite}${p}/#download`}>{t.nav.download}</a>
    </div>
  </div>
</header>

<script>
  document.querySelectorAll('[data-theme-toggle]').forEach((btn) => {
    btn.addEventListener('click', () => {
      const next = document.documentElement.dataset.theme === 'light' ? 'dark' : 'light';
      document.documentElement.dataset.theme = next;
      localStorage.setItem('theme', next);
    });
  });

  document.addEventListener('click', (e) => {
    document.querySelectorAll('[data-lang-menu][open], [data-nav-menu][open], [data-mobile-menu][open]').forEach((menu) => {
      if (e.target instanceof Node && !menu.contains(e.target)) {
        menu.removeAttribute('open');
      }
    });
  });

  document.querySelectorAll('[data-mobile-menu] a').forEach((link) => {
    link.addEventListener('click', () => {
      link.closest('[data-mobile-menu]')?.removeAttribute('open');
    });
  });

  // The header shares the page background, so the hairline only appears once
  // the content has scrolled under it.
  const header = document.querySelector('.site-header');
  if (header) {
    const sync = () => header.classList.toggle('is-scrolled', window.scrollY > 4);
    sync();
    window.addEventListener('scroll', sync, { passive: true });
  }
</script>

<style>
  .site-header {
    position: sticky;
    top: 0;
    z-index: 40;
    background: var(--header-bg);
    border-bottom: 1px solid transparent;
    transition: border-color 0.2s ease;
  }

  .site-header.is-scrolled {
    border-bottom-color: var(--border-color);
  }

  .header-inner {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 1.25rem;
    min-height: 3.75rem;
    padding-block: 0.55rem;
  }

  .brand {
    display: inline-flex;
    align-items: center;
    gap: 0.55rem;
    text-decoration: none;
    font-family: var(--font-display);
    font-size: 1.1rem;
    font-weight: 700;
    letter-spacing: -0.02em;
    padding: 0.35rem 0.5rem 0.35rem 0.35rem;
    margin-left: -0.35rem;
    border-radius: var(--radius-sm);
    transition: background 0.12s ease;
  }

  .brand:hover {
    background: var(--surface-2);
  }

  .brand strong {
    color: var(--accent);
    font-weight: 700;
  }

  .main-nav {
    display: flex;
    align-items: center;
    gap: 0.15rem;
  }

  .main-nav a,
  .nav-menu summary {
    display: inline-flex;
    align-items: center;
    gap: 0.3rem;
    padding: 0.45rem 0.7rem;
    border-radius: var(--radius-sm);
    color: var(--ink);
    font-size: 0.925rem;
    font-weight: 500;
    text-decoration: none;
    cursor: pointer;
    user-select: none;
    transition: background 0.12s ease;
  }

  .main-nav a:hover,
  .nav-menu summary:hover,
  .nav-menu[open] summary {
    background: var(--surface-2);
    color: var(--ink);
  }

  .nav-menu {
    position: relative;
    display: inline-flex;
  }

  .nav-menu summary {
    list-style: none;
  }

  .nav-menu summary::-webkit-details-marker {
    display: none;
  }

  .nav-menu[open] summary svg {
    transform: rotate(180deg);
  }

  .nav-menu ul {
    position: absolute;
    left: 50%;
    transform: translateX(-50%);
    top: calc(100% + 0.5rem);
    margin: 0;
    padding: 0.375rem;
    list-style: none;
    min-width: 11rem;
    display: grid;
    gap: 0.15rem;
    background: var(--surface-1);
    border: 1px solid var(--border-color);
    border-radius: var(--radius);
    box-shadow: 0 1px 2px rgb(0 0 0 / 0.06), 0 10px 28px rgb(0 0 0 / 0.14);
    z-index: 50;
  }

  .nav-menu ul a {
    display: block;
    padding: 0.5rem 0.7rem;
    border-radius: var(--radius-sm);
    font-size: 0.9rem;
    text-decoration: none;
    color: var(--ink-dim);
  }

  .nav-menu ul a:hover {
    background: var(--surface-2);
    color: var(--ink);
  }

  .header-actions {
    display: flex;
    align-items: center;
    gap: 0.4rem;
  }

  /* Headerbar separator between navigation and actions. */
  .header-actions::before {
    content: '';
    width: 1px;
    height: 1.35rem;
    background: var(--border-color);
    margin-inline: 0.5rem 0.25rem;
  }

  .theme-toggle {
    width: 2.25rem;
  }

  .theme-toggle .icon-sun { display: none; }
  :global(html[data-theme='dark']) .theme-toggle .icon-sun { display: block; }
  :global(html[data-theme='dark']) .theme-toggle .icon-moon { display: none; }

  .lang-menu {
    position: relative;
  }

  .mobile-menu {
    display: none;
    position: relative;
  }

  /* Flat headerbar controls — no outlined box. */
  .theme-toggle,
  .lang-menu summary,
  .mobile-menu summary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 0.35rem;
    min-height: 2.25rem;
    border: none;
    background: transparent;
    border-radius: var(--radius-sm);
    color: var(--ink-dim);
    cursor: pointer;
    user-select: none;
    list-style: none;
    transition: background 0.12s ease, color 0.12s ease;
  }

  .mobile-menu summary {
    width: 2.25rem;
  }

  .lang-menu summary {
    padding-inline: 0.6rem;
    font-size: 0.8rem;
  }

  .theme-toggle:hover,
  .lang-menu summary:hover,
  .mobile-menu summary:hover,
  .lang-menu[open] summary,
  .mobile-menu[open] summary {
    background: var(--surface-2);
    color: var(--ink);
  }

  .lang-menu summary::-webkit-details-marker,
  .mobile-menu summary::-webkit-details-marker {
    display: none;
  }

  .mobile-panel {
    position: absolute;
    right: 0;
    top: calc(100% + 0.5rem);
    width: min(18rem, calc(100vw - 2rem));
    padding: 0.375rem;
    display: grid;
    gap: 0.45rem;
    background: var(--surface-1);
    border: 1px solid var(--border-color);
    border-radius: var(--radius);
    box-shadow: 0 1px 2px rgb(0 0 0 / 0.06), 0 10px 28px rgb(0 0 0 / 0.14);
    z-index: 60;
  }

  .mobile-panel nav {
    display: grid;
    gap: 0.1rem;
  }

  .mobile-panel a {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    min-height: 2.35rem;
    padding: 0.55rem 0.7rem;
    border-radius: var(--radius-sm);
    color: var(--ink-dim);
    text-decoration: none;
    font-size: 0.95rem;
  }

  .mobile-panel a:hover {
    background: var(--surface-2);
    color: var(--ink);
  }

  /* Only surfaces below 560px, where the header lang menu is hidden. */
  .mobile-langs {
    display: none;
    gap: 0.1rem;
    padding-top: 0.45rem;
    border-top: 1px solid var(--border-color);
  }

  .mobile-langs a[aria-current='true'] {
    color: var(--accent);
    font-weight: 600;
  }

  .mobile-actions {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.45rem;
    padding-top: 0.45rem;
    border-top: 1px solid var(--border-color);
  }

  .mobile-actions .btn {
    justify-content: center;
    color: inherit;
  }

  .mobile-actions .btn-primary {
    color: var(--accent-ink);
  }

  .lang-menu ul {
    position: absolute;
    right: 0;
    top: calc(100% + 0.5rem);
    margin: 0;
    padding: 0.375rem;
    list-style: none;
    min-width: 11rem;
    display: grid;
    gap: 0.15rem;
    background: var(--surface-1);
    border: 1px solid var(--border-color);
    border-radius: var(--radius);
    box-shadow: 0 1px 2px rgb(0 0 0 / 0.06), 0 10px 28px rgb(0 0 0 / 0.14);
    z-index: 50;
  }

  .lang-menu ul a {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    padding: 0.5rem 0.7rem;
    border-radius: var(--radius-sm);
    text-decoration: none;
    font-size: 0.9rem;
    color: var(--ink-dim);
  }

  .lang-menu ul a:hover {
    background: var(--surface-2);
    color: var(--ink);
  }

  .lang-menu ul a[aria-current='true'] {
    color: var(--accent);
    font-weight: 600;
  }

  .flag {
    font-size: 1rem;
    line-height: 1;
  }

  @media (max-width: 980px) {
    .main-nav {
      display: none;
    }

    .mobile-menu {
      display: block;
    }
  }

  @media (max-width: 560px) {
    .lang-menu {
      display: none;
    }

    .mobile-langs {
      display: grid;
    }

    .header-actions::before {
      display: none;
    }

    .header-actions > .btn-primary {
      padding-inline: 1rem;
    }
  }

  @media (max-width: 420px) {
    .header-inner {
      gap: 0.8rem;
    }

    .brand span {
      font-size: 1rem;
    }

    .header-actions {
      gap: 0.25rem;
    }
  }
</style>
